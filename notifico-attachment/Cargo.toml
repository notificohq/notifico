[package]
name = "notifico-attachment"
version = "0.1.0"
edition = "2024"

[dependencies]
notifico-core = { path = "../notifico-core" }

serde = { version = "1.0.219", features = ["derive"] }
async-trait = "0.1.88"
tokio = { version = "1.44.2", features = ["fs", "sync"] }
mime_guess = "2.0.5"
